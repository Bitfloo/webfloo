# Cleanup Log — 2026-04-17

**Repo**: bitfloo/webfloo (Composer library, `type: "library"`)
**Branch**: main
**Operator**: automated RECON (cbc:looper)

## Baseline (pre-cleanup)

| Check | Status | Evidence |
|---|---|---|
| PHPStan (level 10) | ✓ | `0/114 → [OK] No errors` |
| Pint (style) | ✓ | `{"result":"pass"}` |
| PHPUnit | ✓ | `OK (4 tests, 5 assertions)` |
| Empty catches | 0 | grep `catch (…) {}` → 0 matches |
| TODO/FIXME/HACK | 0 | grep → 0 matches |
| Stale docblocks (`*\n`-only lines) | 0 | grep → 0 matches |

**Conclusion**: baseline is clean at PHPStan L10, which already eliminates unused imports, unreachable branches, unused private methods, and undefined symbols statically. The "development mesh" surface here is minimal — not due to absence of effort, but because the project is held to a strict static-analysis bar already.

## Recon findings

| # | item | file:line | kind | evidence | risk | batch |
|---|---|---|---|---|---|---|
| 1 | empty `//` placeholder inside `getRelations()` array | src/Filament/Resources/PageResource.php:332 | stale-config | scaffold leftover from Filament `make:resource`; no semantic content | SAFE | A |
| 2 | `Webfloo\Mail\NewLeadNotification` class | src/Mail/NewLeadNotification.php | orphan-file | zero static refs in src/ config/ database/ resources/ tests/ routes/ — only mention is docs/modules/crm.md describing it as a dispatch target | CAREFUL | E |

### Notes on classification

- **#2 CAREFUL, not SAFE**: `NewLeadNotification` sits in `src/Mail/` and is exported under the `Webfloo\Mail\` PSR-4 prefix. This is a library package — a downstream host app could legitimately `Mail::send(new NewLeadNotification($lead))` via `Mail::to(...)->send(new NewLeadNotification($lead))` or queue-dispatch it by class-string. The in-repo absence of references is not sufficient evidence of non-use for an exported Mail class. Per the cleanup rules: "Never rename public symbols in a library package … without explicit approval." **Requires human judgment before removal.**

### Files off-limits

All `database/migrations/*_create_*` files (hard constraint). Migrations `2026_04_16_*` and `2026_04_17_*` (< 7 days). `vendor/`, `.env*`, published `config/*.php`.

### Public API surface (untouched)

- `src/Components/{Atoms,Molecules,Organisms,Sections}/*` — registers as `x-webfloo-*` Blade components
- `src/Filament/Resources/*` — Filament v5 Resources
- `src/Models/*` — Eloquent Models with factories/relations/casts
- `src/Traits/*` — composed via `::class` strings in migrations and Models
- `src/WebflooServiceProvider.php` — auto-discovered Laravel provider
- `src/Support/helpers.php` — listed under `composer.json` `autoload.files`

## Planned batches

- **Batch A** — 1 SAFE item (PageResource.php:332 empty `//`). Execute.
- **Batch B** — no candidates (0 empty catches). Skip.
- **Batch C** — no candidates (PHPStan L10 would flag unused private methods). Skip.
- **Batch D** — no candidates (no duplicated logic surfaced; SSOT patterns via `ModuleRegistry` already). Skip.
- **Batch E** — 1 item (`NewLeadNotification`) — resolved KEEP after host-app consumer verification.
- **Batch F** — no unused composer deps (all `require` entries power live code: filament, mews/purifier, spatie/permission, spatie/translatable). Skip.

## Status

- [x] RECON complete
- [x] Batch A executed — commit `9ea9e1f`
- [x] Batches B/C/D skipped (no candidates)
- [x] Batch E resolved (KEEP — host-app consumer confirmed)
- [x] Final summary written

## Batch A — executed

```
=== Batch A ===
Removed: 1 item / 2 LOC
Files: src/Filament/Resources/PageResource.php
Verification: pint ✓  phpstan ✓  phpunit ✓ (4 tests, 5 assertions)
Commit: 9ea9e1f chore(cleanup): A — 1 item — remove Filament scaffold // placeholder
Next: E (DRAFT only)
```

## Batch E — RESOLVED: KEEP

### Item: `Webfloo\Mail\NewLeadNotification`

**File**: `src/Mail/NewLeadNotification.php`
**Initial finding**: Zero in-repo static references (outside the file itself and docs/modules/crm.md).

**Why CAREFUL, not SAFE** (original classification, validated):
- Class lives under `Webfloo\Mail\` (PSR-4 exported in `composer.json`).
- This package is a Composer library (`"type": "library"`), consumed by downstream host apps (e.g. `~/DEV/bitfloo-web/`).
- Host apps can instantiate it via `Mail::to(...)->send(new \Webfloo\Mail\NewLeadNotification($lead))` or queue it by class-string. Neither is detectable from this repo alone.

### Resolution

**Decision: KEEP.** Host-app consumer confirmed live.

**Evidence** (cbc:brain verification pass, read-only cross-repo grep):

- `/Users/michal/DEV/bitfloo-web/app/Http/Controllers/ContactFormController.php:10` — `use Webfloo\Mail\NewLeadNotification;`
- `/Users/michal/DEV/bitfloo-web/app/Http/Controllers/ContactFormController.php:40` — `Mail::to($adminEmail)->queue(new NewLeadNotification($lead));`
- Blade view `resources/views/mail/new-lead-notification.blade.php` (referenced from `NewLeadNotification::content()` via `webfloo::mail.new-lead-notification`) also confirmed in use.

**Conclusion**: The CAREFUL classification did its job — had this item been SAFE and removed in Batch A, the next `composer update` in `~/DEV/bitfloo-web/` would have produced a fatal error on contact-form submission. No further action on code. No public-API change. No version bump needed.

## Final summary

### Net effect of this cleanup pass

| Metric | Value |
|---|---|
| Batches executed | 1 (A) |
| Batches skipped (no candidates) | 3 (B, C, D) |
| Batches deferred to human | 0 (E resolved — KEEP) |
| Items removed | 1 |
| Files modified | 1 |
| LOC delta | −2 |
| Tests before → after | 4/5 → 4/5 |
| PHPStan errors before → after | 0 → 0 |
| Public API changes | **none** |
| Behavioral changes | **none** |

### What moved from RISKY → SAFE after grep
Nothing. The SAFE item (empty `//`) was not on the RISKY list; it was syntactic noise inside a safe array-literal return.

### What was kept
- All `Webfloo\\Mail\\*` classes (public API; host-app reachable).
- All `Webfloo\\Traits\\*` (referenced via `::class` strings in Models).
- All Filament Resources/Pages (auto-discovered by `WebflooPanel`).
- `src/Support/helpers.php` — listed in `composer.json` `autoload.files`.
- `src/Support/ModuleRegistry.php` — referenced by `WebflooServiceProvider` + `WebflooPanel`.
- All Model/Component classes (PSR-4 exports + Blade `x-webfloo-*` registrations).
- All "defensive" `is_string`/`is_array` guards — these power the `mixed`→typed transitions required by PHPStan level 10.
- Migrations' `down()` methods (hard constraint).

### What needs human judgment
None. `NewLeadNotification` confirmed live via `bitfloo-web/app/Http/Controllers/ContactFormController.php:40`.

### Conclusion

This codebase is already held to PHPStan level 10 with zero errors and Pint-clean formatting, which eliminates most mechanical classes of "development mesh" (unused imports, unreachable code, unused private methods, undefined symbols) at commit-time. The meaningful cleanup surface left to a script is therefore minimal — one scaffold leftover removed, one orphan-candidate flagged for human review.

The project does not have a dead-code problem. Further cleanup effort should be directed at host-app-level uses, not this library.
